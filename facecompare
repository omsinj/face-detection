import imageio
from skimage.metrics import structural_similarity as ssim
import matplotlib.pyplot as plt

def compare_images(image_path1, imagepath2):
image1 = imageio.imread(imapge pth1)
image.imread(path2)
ssi_index, _= ssim(image1, image2, full = True)

return ssi_index

def plot_image(image, image2):
fig, (ax1, ax2) = plt.subplots(1, figsize = (10,5))
ax1.imshow(image1, cmap='gray')


xxxx more to follow xxxx
